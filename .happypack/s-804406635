'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = fetchSignUp;

var _signup_action = require('../actions/signup_action');

const initialState = {
  response: ''
};

// receiving action response

function fetchSignUp(state = initialState, action) {
  switch (action.type) {
    case 'SIGNUP':
      return {
        response: action.payload
      };
      break;
    case 'ERROR':
      return {
        response: action.payload
      };
    default:
      return state;
  }
}